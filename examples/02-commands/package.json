{
	"name": "nestwhats-example-02-commands",
	"version": "0.0.0",
	"description": "Arguments, aliases, DTOs and subcommand groups.",
	"private": true,
	"license": "GPL-3.0",
	"type": "module",
	"scripts": {
		"build": "tsc -p tsconfig.build.json",
		"start": "node dist/main.js",
		"start:dev": "tsc -p tsconfig.build.json --watch",
		"typecheck": "tsc -p tsconfig.build.json --noEmit"
	},
	"dependencies": {
		"@nestjs/common": "^12.0.0",
		"@nestjs/core": "^12.0.0",
		"nestwhats": "^4.0.0",
		"reflect-metadata": "^0.2.2",
		"rxjs": "^7.8.1",
		"@nestwhats/platform-whatsapp-web.js": "^4.0.0",
		"whatsapp-web.js": "^1.34.0"
	},
	"devDependencies": {
		"@types/node": "^22.0.0",
		"typescript": "^5.9.0"
	},
	"engines": {
		"node": ">=20.19.0"
	}
}
