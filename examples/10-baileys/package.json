{
	"name": "nestwhats-example-10-baileys",
	"version": "0.0.0",
	"description": "What a socket platform reaches that a browser session cannot.",
	"private": true,
	"license": "GPL-3.0",
	"type": "module",
	"scripts": {
		"build": "tsc -p tsconfig.build.json",
		"start": "node dist/main.js",
		"start:dev": "tsc -p tsconfig.build.json --watch",
		"typecheck": "tsc -p tsconfig.build.json --noEmit"
	},
	"dependencies": {
		"@nestjs/common": "^12.0.0",
		"@nestjs/core": "^12.0.0",
		"nestwhats": "^4.0.0",
		"reflect-metadata": "^0.2.2",
		"rxjs": "^7.8.1",
		"@nestwhats/platform-baileys": "^4.0.0",
		"@whiskeysockets/baileys": "^7.0.0-rc14"
	},
	"devDependencies": {
		"@types/node": "^22.0.0",
		"typescript": "^5.9.0"
	},
	"engines": {
		"node": ">=20.19.0"
	}
}
