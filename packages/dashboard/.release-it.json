{
	"git": {
		"commitMessage": "chore(dashboard): release dashboard-v${version}",
		"requireBranch": "master",
		"requireCommits": true,
		"tagName": "dashboard-v${version}"
	},
	"github": {
		"release": true,
		"releaseName": "@nestwhats/dashboard v${version}",
		"releaseNotes": "git cliff --tag dashboard-v${version} --unreleased --strip all -c ../../cliff.toml -r ../.. --include-path 'packages/dashboard/**' --tag-pattern '^dashboard-v'",
		"assets": ["nestwhats-dashboard-*.tgz"]
	},
	"npm": {
		"publish": true
	},
	"hooks": {
		"before:bump": "yarn run -T nx build dashboard",
		"after:bump": "yarn run -T biome format --write package.json && git add package.json && git cliff --unreleased --prepend ./CHANGELOG.md --tag dashboard-v${version} -c ../../cliff.toml -r ../.. --include-path 'packages/dashboard/**' --tag-pattern '^dashboard-v' && git add CHANGELOG.md && rm -f nestwhats-dashboard-*.tgz && npm pack"
	}
}
