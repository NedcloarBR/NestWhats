{
	"git": {
		"commitMessage": "chore(locale): release locale-v${version}",
		"requireBranch": "master",
		"requireCommits": true,
		"tagName": "locale-v${version}"
	},
	"github": {
		"release": true,
		"releaseName": "@nestwhats/locale v${version}",
		"releaseNotes": "git cliff --tag locale-v${version} --unreleased --strip all -c ../../cliff.toml -r ../.. --include-path 'packages/locale/**'",
		"assets": ["nestwhats-locale-*.tgz"]
	},
	"npm": {
		"publish": true
	},
	"hooks": {
		"before:bump": "yarn run -T nx build locale",
		"after:bump": "biome format --write package.json && git add package.json && git cliff --unreleased --prepend ./CHANGELOG.md --tag locale-v${version} -c ../../cliff.toml -r ../.. --include-path 'packages/locale/**' && git add CHANGELOG.md && rm -f nestwhats-locale-*.tgz && npm pack"
	}
}
