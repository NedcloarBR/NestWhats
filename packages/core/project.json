{
	"name": "core",
	"$schema": "../../node_modules/nx/schemas/project-schema.json",
	"sourceRoot": "packages/core/src",
	"projectType": "library",
	"tags": ["scope:core", "type:library"],
	"targets": {
		"build": {
			"executor": "nx:run-commands",
			"outputs": ["{projectRoot}/dist"],
			"options": {
				"command": "rimraf dist && tsc -p tsconfig.build.json",
				"cwd": "{projectRoot}"
			}
		},
		"typecheck": {
			"executor": "nx:run-commands",
			"options": {
				"command": "tsc -p tsconfig.build.json --noEmit && tsc -p tsconfig.spec.json",
				"cwd": "{projectRoot}"
			}
		},
		"test": {
			"executor": "nx:run-commands",
			"options": {
				"command": "vitest run",
				"cwd": "{projectRoot}"
			}
		}
	}
}
