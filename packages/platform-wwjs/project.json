{
	"name": "platform-wwjs",
	"$schema": "../../node_modules/nx/schemas/project-schema.json",
	"sourceRoot": "packages/platform-wwjs/src",
	"projectType": "library",
	"tags": ["scope:platform-wwjs", "type:library"],
	"targets": {
		"build": {
			"executor": "nx:run-commands",
			"outputs": ["{projectRoot}/dist"],
			"dependsOn": ["^build"],
			"options": {
				"command": "rimraf dist && tsc -p tsconfig.build.json",
				"cwd": "{projectRoot}"
			}
		},
		"typecheck": {
			"executor": "nx:run-commands",
			"options": {
				"command": "tsc -p tsconfig.build.json --noEmit",
				"cwd": "{projectRoot}"
			}
		}
	}
}
